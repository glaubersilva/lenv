; Base PHP overrides for every lenv project (sync_lando_php_ini).
; uopz is installed by ensure-dev-extensions.sh for plugin test suites.
; Default uopz.exit=0 disables PHP exit/die and breaks admin redirects in some plugins.
uopz.exit=1
