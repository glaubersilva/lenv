; Appended when xdebug is set to "debug" in .lando.yml (lenv new / lenv update).
; When xdebug is "off", enable at runtime with: lenv xdebug on (writes a separate runtime ini).
xdebug.mode=debug
xdebug.start_with_request=yes
xdebug.client_host=host.lando.internal
